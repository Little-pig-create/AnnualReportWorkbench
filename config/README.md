# Config Files

本目录用于存放项目级样例配置和辅助清单。

## 当前文件

- `update.json.example`：远端更新清单样例
- `webview_config.example.json`：桌面端运行配置样例

## 说明

- 实际运行时的桌面端配置不会读取本目录
- 桌面端真实配置保存位置为 `%APPDATA%\\AnnualReportWorkbench\\webview_config.json`
- 本地历史运行记录通常位于 `%APPDATA%\\AnnualReportWorkbench\\run_history.json`
